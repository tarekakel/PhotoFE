import { DownloadPerTypePipe } from './download-per-type.pipe';

describe('DownloadPerTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DownloadPerTypePipe();
    expect(pipe).toBeTruthy();
  });
});
